# NL2SQL Voice Assistant - Client-Only Dependencies
# For remote clients connecting to a NL2SQL server
# NO GPU required, NO Ollama, NO Whisper, NO PostgreSQL

# GUI
PyQt6>=6.5.0

# HTTP client (for remote server)
requests>=2.28.0

# Audio recording (for voice input)
sounddevice>=0.4.6
numpy>=1.24.0

# Data handling
pandas>=2.0.0

# Export formats
reportlab>=4.0.0
openpyxl>=3.1.0
matplotlib>=3.7.0

# ============================================
# USAGE:
# ============================================
# 1. Install dependencies:
#    pip install -r requirements_client.txt
#
# 2. Run in remote mode:
#    python main.py --server https://your-server-url.trycloudflare.com
